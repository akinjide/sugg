machine:
  node:
    version: 6.11.1
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - npm install gulp -g
    - npm install bower -g
  post:
    - bower install
    - npm install

test:
  override:
    - gulp nsp

deployment:
  development:
    branch: staging
    heroku:
      appname: sugg-dev

#  production:
#    branch: master
#    heroku:
#      appname: sugg
