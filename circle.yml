dependencies:
  pre:
    - bower install
    - npm install
  post:
    - npm install module-deps
    - npm install gulp -g

test:
override:
#   - gulp browserify
  - gulp production

deployment:
  development:
    branch: staging
    heroku:
      appname: znote-dev

#  production:
#    branch: master
#    heroku:
#      appname: znote
