dependencies:
  pre:
    - bower install
    - npm install
  post:
    - npm install module-deps
    - npm install gulp -g

test:
  override:
    - gulp nsp

deployment:
  development:
    branch: staging
    commands:
      - gulp production
    heroku:
      appname: znote-dev

#  production:
#    branch: master
#    heroku:
#      appname: znote
