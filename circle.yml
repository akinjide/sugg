dependencies:
  pre:
    - bower install
  post:
    - npm install module-deps
    - npm install gulp -g

test:
  override:
    - gulp browserify
    - gulp test

deployment:
  development: 
    branch: develop
    heroku:
      appname: znote-dev

  production:
    branch: master
    heroku:
      appname: znote
