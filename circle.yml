machine:
  node:
    version: 6.1.0
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - npm install gulp -g
    - npm install bower -g
  post:
    - bower install
    - npm install

test:
  override:
    - gulp nsp

deployment:
  development:
    branch: staging
    commands:
      - gulp production
    heroku:
      appname: znote-dev

#  production:
#    branch: master
#    heroku:
#      appname: znote
